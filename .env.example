# Database - PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DB_URL="postgresql://user:password@localhost:5432/impossible_quiz"

# Pusher Configuration
# Get these from https://dashboard.pusher.com/
PUSHER_APP_ID="your_app_id"
PUSHER_SECRET="your_secret_key"

# Public Pusher keys (accessible in browser)
NEXT_PUBLIC_PUSHER_KEY="your_public_key"
NEXT_PUBLIC_PUSHER_CLUSTER="your_cluster"
